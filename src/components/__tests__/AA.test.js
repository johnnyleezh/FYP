  test("Obtain student list", done => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1000); // 5 seconds delay
  });

  test("Student details", done => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 2000); // 5 seconds delay
  });